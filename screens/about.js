import React from "react";
import { StyleSheet, Text } from "react-native";

export default function About() {
  return <Text style={styles.container}>About Screen</Text>;
}
const styles = StyleSheet.create({
  container: {
    padding: 24,
  },
});
